{
    IBClasses = (
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }, 
        {
            CLASS = HeliosDocument; 
            LANGUAGE = ObjC; 
            OUTLETS = {
                "_osgView" = OSGView; 
                "_progressBar" = NSProgressIndicator; 
                "_statusText" = NSTextField; 
            }; 
            SUPERCLASS = NSDocument; 
        }, 
        {
            ACTIONS = {
                activateColorPanel = id; 
                enablePerFacetLighting = id; 
                enablePerVertexLighting = id; 
                fitModel = id; 
                resetCamera = id; 
                stereo = id; 
                toggleAxis = id; 
                toggleBoundingBox = id; 
                toggleBoundingSphere = id; 
                toggleDisplayLists = id; 
                toggleHiddenLines = id; 
                toggleLights = id; 
                toggleModelSpin = id; 
                toggleShadeModel = id; 
                toggleTwoSidedLighting = id; 
                updateBackgroundColor = id; 
            }; 
            CLASS = OSGView; 
            LANGUAGE = ObjC; 
            SUPERCLASS = NSOpenGLView; 
        }, 
        {
            CLASS = UKToolbarFactory; 
            LANGUAGE = ObjC; 
            OUTLETS = {owner = NSWindow; }; 
            SUPERCLASS = NSObject; 
        }, 
        {
            ACTIONS = {setDisplayLists = id; setTwoSidedLighting = id; }; 
            CLASS = ViewerControl; 
            LANGUAGE = ObjC; 
            SUPERCLASS = NSObject; 
        }
    ); 
    IBVersion = 1; 
}