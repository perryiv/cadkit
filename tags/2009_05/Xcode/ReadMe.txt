If this is the first time building with the Xcode projects, copy Variables.xconfig.in to 
Variables.xconfig.  Then modify the variables to reflect the paths on your computer.  This is 
still a work in progress, so there are missing variables and not all the targets are updated.
